/* 
  Implement a page which makes a get request to https://reqres.in/ to get a list of users and display them when page loads
  feel free to accomplish this goal however you wish, within the context of react.
  feel free to use any library you like

  api.js file has already been created for you. Feel free to use it or not.


  1:  Add a button for each user to make a delete request to delete that user.
      Update the displayed users excluding the deleted user.

  2: Make a filter box to filter the displayed users by name.
*/

import React from "react";

import "./styles.css";

const App: React.FC = () => {
  return <div className="App">React Code Challenge</div>;
};

export default App;
